# Google AI Studio API key: https://aistudio.google.com/apikey
# Free tier, no credit card. Server-side only — never prefix this NEXT_PUBLIC_.
GEMINI_API_KEY=

# Optional. Overrides the default model in utils/constants.ts.
# List what your key can actually reach:
#   curl -s https://generativelanguage.googleapis.com/v1beta/openai/models \
#     -H "Authorization: Bearer $GEMINI_API_KEY"
# NEXT_PUBLIC_CHAT_MODEL=gemini-3.5-flash
